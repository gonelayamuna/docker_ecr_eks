pipeline {
    agent 'aws-slave'

    stages {
        stage('Test')   {
            steps {
                script {
                    echo "First build"
                }
            }
        }
    }
}
