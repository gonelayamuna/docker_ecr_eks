pipeline {
    agent {
        label 'aws-slave'  // Specifies the default agent for the entire pipeline
    }


    stages {
        stage('Test')   {
            steps {
                script {
                    echo "First build"
                }
            }
        }
    }
}
